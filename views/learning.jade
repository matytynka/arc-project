extends layout

block content
    div(id="learningdiv")
        h4 Spanish
        h2= foreign
        h4 Polish
        input(type="text", id="answer")
        br
        button(type="button", onclick="answer()") Nie wiem
        button(type="button", onclick="check()") Sprawdź
        script.
            function answer() {
                alert("Anwser is !{local}");
            }

            function check() {
                const answer = document.getElementById("answer").value;
                if(answer === "!{local}") alert("Dobrze!");
            }